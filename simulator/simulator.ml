(* simulator code will be here. *)

(* simulator state *)
(*let t = {
  lambdamans: Lambdaman.t array;
  ghosts:    Ghost.t array;
  field:     World.t;
}

let make lambdamans ghosts world = {
  lambdamans = lambdamans;
  ghosts     = ghosts;
  field      = field;
}

let tick =
  failwith "Not implemented yet"
*)
